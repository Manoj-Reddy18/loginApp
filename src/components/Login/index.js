import './index.css'

const login = () => {
  btnContent = () => {
    clickLogin()
  }
  return (
    <button type="button" onClick={this.btnContent}>
      Login
    </button>
  )
}

export default login
