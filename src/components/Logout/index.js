import './index.css'

const logout = () => {
  btnContent = () => {
    clickLogout()
  }
  return (
    <button type="button" onClick={this.btnContent}>
      Logout
    </button>
  )
}

export default logout
